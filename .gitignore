node_modules/
dist/
dist-ssr/
*.local

# build output
**/dist/
**/.vite/

# tauri (added later)
**/src-tauri/target/

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# secrets / local env (never commit; .env.example is the tracked template)
.env
.env.*
!.env.example

# libmpv runtime DLL (place locally for tauri build; not committed)
**/src-tauri/*.dll
